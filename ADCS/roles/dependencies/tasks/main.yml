---
- name: Playbook to install Microsoft AD collection
  hosts: all
  become: yes
- name: Install Microsoft AD collection
  ansible.builtin.shell: "whoami" # "ansible-galaxy collection install microsoft.ad"
